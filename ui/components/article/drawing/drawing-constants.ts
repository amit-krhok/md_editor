export const MAX_UNDO = 35;
export const BRUSH_PX = 4;
export const ERASER_PX = 28;

export const CANVAS_MIN_WIDTH_CSS_PX = 240;
export const CANVAS_HEIGHT_CSS_PX = 320;
/** Inset so canvas + border stays inside the padded wrapper (1px border ≈ 2px total). */
export const CANVAS_BORDER_INSET_CSS_PX = 2;
export const RESIZE_DEBOUNCE_MS = 120;
export const MAX_DEVICE_PIXEL_RATIO = 2;
